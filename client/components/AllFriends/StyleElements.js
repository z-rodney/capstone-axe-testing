import styled from 'styled-components';
import { Container } from '../styledComponents';

export const WideColumnContainer = styled(Container)`
  flex-flow: column nowrap;
  width: 85%;
  align-self: center;
`

export const Graph = styled.div`
  height: 40em;
`
